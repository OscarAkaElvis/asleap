# Utility scripts

Auxiliary scripts created while working on asleap:

 - `morewords.pl`: Reads a dictionary file and appends `00` through `99` to each entry.
 - `strip-nonascii.pl`: Removes non-printable characters from the input file.
 - `testidx.c`: Reports discrepancies between a genkeys data file and index file.
