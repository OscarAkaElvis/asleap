# Sample packet capture files for testing

 - The password for the LEAP exchange in `joshlea.dump` is `password`.
 - The password for the LEAP exchange in `leap.dump` is `qaleap`.
 - The password for the LEAP exchange in `leap2.dump` is `blamo`.
 - The password for the PPTP exchange in `pptp.dump` is `turquoise`.
